[package]
name    = "reviva-prompts"
version.workspace = true
edition.workspace = true

# See docs/module.md §reviva-prompts
[dependencies]
reviva-core = { workspace = true }

[dev-dependencies]
insta = { workspace = true }

